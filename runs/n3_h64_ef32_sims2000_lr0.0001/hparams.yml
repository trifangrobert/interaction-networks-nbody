# Data — 4x more simulations to reduce overfitting
n_train_sims: 2000
n_val_sims: 200
n_bodies: 3
n_steps: 100
dt: 0.01

# Model — smaller capacity to match the harder generalization task
hidden_dim: 64
effect_dim: 32

# Training — more epochs since dataset is larger
lr: 0.0001
epochs: 200
batch_size: 256

# Output
output_dir: ../runs
